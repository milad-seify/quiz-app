class Question {
  // late List<String> question;
  // late List<bool> answer;
  String question;
  bool answer;
  String completeAnswer;
  Question(this.question, this.answer, this.completeAnswer);
}
